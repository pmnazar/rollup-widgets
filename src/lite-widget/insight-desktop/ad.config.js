define(
    'ad.config',
    function() {
        var config;

        config = {
            aol: {
                website: {
                    server: 'adserver.adtechus.com',
                    network: '5524.1',
                    secure: true
                }
            }
        };

        return config;
    }
);